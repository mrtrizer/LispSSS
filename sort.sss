setq list (quote (a b c (d e f)));
print  (cdr (cdr (cdr list)));

defun printList (list)
{
    if (== list nil)(return "End!");
    print "Element: " (car list);
    printList (cdr list);
};

printList list;
